<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.call_state_handler">
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <!-- 
    Note: For enhanced video call detection, apps can optionally add these permissions:
    - PACKAGE_USAGE_STATS: Requires user to manually grant in Settings > Apps > Special access > Usage access
    - QUERY_ALL_PACKAGES: Requires Play Store justification for Android 11+
    
    The plugin works without these permissions using audio mode detection.
    See README for more details.
    -->
</manifest>